Used to configure the default role for the `accept` command.<br />
syntax: `!dr (@roleMention)`<br />
<br />
`(@roleMention)` - sets the default role, or clears all if no role is provided.<br />
<br />
`MANAGE_ROLES` permission required.